# copy to .streamlit/secrets.toml for local runs, and paste the same thing into
# the secrets box on streamlit cloud. the real file is gitignored.

[connections.snowflake]
account = "po08310.me-central2.gcp"
user = "THOPL0"
password = ""
role = "ACCOUNTADMIN"
warehouse = "COMPUTE_WH"
database = "SHELTER"
schema = "ANALYTICS"
# ? placeholders bound server-side. without this the connector leaves them in the sql.
paramstyle = "qmark"
